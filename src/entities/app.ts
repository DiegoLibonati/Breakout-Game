export type MoveKeys = "ArrowLeft" | "ArrowRight";

export type Coords = { x: number; y: number };

export type Sizes = { width: number; height: number };

export type Rows = { y1: number; y2: number; y3: number };
